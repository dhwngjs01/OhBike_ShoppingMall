.wrap1280
	#productListTop
		span.productCount
			span.text-red #{productCount.numberWithCommas()} 
			|개의 상품이 있습니다.
		span.productBrand
			|(브랜드 
			span.text-red 전체
			|에서 상품)
	#productList
		- let maxCount = 5;

		each product, i in productList
			- if(i % maxCount == 0)
				|<ul class="row">
			
			li.col
				a(href="/public/productDetail?productNo=#{product.product_no}")
					.image
						img(src="../uploadFiles/#{product.file_save_name}", alt="#{product.file_show_name}")
					p #{product.product_brand}
					p #{product.product_name}
					p #{product.product_en_name}
					p.productPrice
						span.text-red #{product.product_price.numberWithCommas()} 
						|원
			- if(i % maxCount == maxCount - 1)
				|</ul>